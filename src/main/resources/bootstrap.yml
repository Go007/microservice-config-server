spring:
  application:
    name: microservice-config-server
  profiles:
    active: dev, demo, prod, devn
#    active: native
  cloud:
    config:
      server:
#        native:
#          search-locations: classpath:config
        git:
          uri: https://gitee.com/hong_007/SpringCloud-Study.git
          skip-ssl-validation: true
          username: Go007
          password: htimeis007money
          search-paths: 'config/{application}'
      label: master
